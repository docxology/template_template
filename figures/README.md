# figures

Placeholder directory for the `template_template` meta-research exemplar.

Architecture figures are **not** tracked here. They are regenerated by Stage 02
analysis from live repository introspection and written to `../output/figures/`
(disposable, gitignored, reproducible). The manuscript references them as
`../output/figures/*.png`, matching the other exemplars.
