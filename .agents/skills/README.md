# .agents/skills

Project-local skill catalog for the `template_template` exemplar. One folder
per skill; each folder ships `SKILL.md`, `AGENTS.md`, and `README.md`.

| Skill | Purpose |
| --- | --- |
| [`template-template/`](template-template/README.md) | Drive this exemplar end-to-end (scripts, gates, regeneration, forking) |

The directory contract (schema for every skill folder) is in
[`AGENTS.md`](AGENTS.md).
